## Installation

To install PhpMyAdmin using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/phpmyadmin%20installer.sh)
```

To install anti ddos using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/anti-ddos.sh)
```

To install backup-pterodactyl using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/auto-backup-pterodactyl.sh)
```

To install auto backup proxmox using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/auto-backup.sh)
```

To install pterodactyl egg import using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/auto-import-egg.sh)
```

To install auto-ssl using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/auto-ssl.sh)
```

To install backup-notify using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/backup-notify.sh)
```

To install grafana using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/grafana-dashboard.sh)
```

To install pterodactyl-egg using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/install-eggs.sh)
```

To install proxmox using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/install-proxmox.sh)
```

To install pterodactyl using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/install-pterodactyl.sh)
```

To install netdata using this script, simply run the following command in your terminal:

```sh
bash <(curl -s https://raw.githubusercontent.com/aldixyz331/installer/refs/heads/main/netdata.sh)
```

This command will download and execute the installer script automatically.

